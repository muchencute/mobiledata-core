language: java
jdk:
  - oraclejdk7
script: "mvn clean package"
notifications:
  email: true
skip_cleanup: true
after_success:
  "curl -u $FTP_USER:$FTP_PASSWORD -T target/mobiledata-core.war ftp://$FTP_IP/webapps/"